version: '2'

services:
  database:
    image: mariadb
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: boom
      MYSQL_DATABASE: test

  consul:
    image: consul
    ports:
      - "8500:8500"
